# PROJECT_SPEC — 프랜차이즈 인사·근태·급여 시스템 (Firebase 기반)

이 문서는 Claude Code에 프로젝트를 시작할 때 그대로 붙여넣거나, 프로젝트 루트에 `PROJECT_SPEC.md`로 저장해두고 참조용으로 쓰기 위해 정리한 것입니다.

---

## 0. 프로젝트 개요

- **목표**: 프랜차이즈 외식업 본사가 매장(10개 이상)의 정직원·아르바이트·일용직 인사·근태를 관리하고, 이를 바탕으로 한국 노동법을 준수하는 급여 계산까지 자동화하는 웹앱
- **기술 스택**: React + Firebase (Firestore, Firebase Auth, Cloud Functions, Hosting)
- **현재 단계**: 구글시트/Apps Script로 개념 검증을 마치고, React 프로토타입(아래 참고)까지 만든 상태. 이제 Firebase 기반 실제 웹앱으로 재구축하는 단계
- **진행 방식**: 1단계(근태·인사 로우데이터를 정확히 수집하는 것)에 집중하고, 대시보드·알림·급여계산 자동화는 2단계로 순차 확장

## 1. 첨부 참고자료

같은 대화에서 만든 아래 자료들을 프로젝트 컨텍스트로 함께 넣어주세요.
- `payroll_flow_prototype.jsx` — 사원등록 게이트 구조, 근태입력, 알림 대시보드를 시연하는 동작하는 React 프로토타입 (Firebase 연동 없이 useState로만 구현됨). **디자인·UX·인터랙션 구조를 최대한 그대로 재현하고, Firebase 연동 로직만 추가하는 방식으로 작업해주세요.**
- 이 스펙 문서(PROJECT_SPEC.md)

## 2. 규모 및 제약조건

- 매장 10개 이상, 전 매장 상시근로자 5인 이상 (근로기준법 전 조항 적용 대상)
- 전 매장 자정(00:00) 이전 영업 종료 → 날짜 경계를 넘는 근무 없음
- 급여 계산 범위는 세전 총지급액까지 (가산수당 포함). 4대보험료·소득세 원천징수는 별도 외부 프로그램에서 처리 (이 시스템 스코프 아님)
- 급여는 매월 말일 마감, 익월 10일 지급

## 3. 사용자 역할 (권한 구조)

| 역할 | 권한 |
|---|---|
| 매장(점장) | 사원등록 최초 입력, 근태 당일 입력·수정 |
| 회계팀 (본사) | 사원등록 중 이름/주민번호/계좌번호/연락처 확인. **인사팀보다 먼저 확인해야 하는 게이트 역할** |
| 인사팀 (본사) | 사원등록 중 보건증/근로계약서 확인. **회계팀 확인이 끝난 건만 조회 가능** |
| 본사 대시보드 | 전체 현황 조회, 각종 알림 확인 |

## 4. 데이터 모델 (Firestore 컬렉션 제안)

### `employees` (사원 마스터)
```
{
  id, name, ssn, phone, hireDate, account, position, dept,
  employmentType: "정직원" | "아르바이트" | "일용직",
  storeCode,
  documents: { idCard: bool, bankbook: bool, healthCert: bool, contract: bool },
  accountingConfirmed: bool, accountingConfirmedAt, accountingConfirmedBy,
  hrConfirmed: bool, hrConfirmedAt, hrConfirmedBy,
  status: "매장입력완료" | "회계팀확인대기" | "인사팀확인대기" | "확정",
  retireDate, retireStatus,
  createdAt, createdBy (매장 사용자)
}
```
**등록 후 매장 사용자는 이 문서를 수정할 수 없음** — Firestore 보안 규칙에서 매장 역할의 update 권한을 원천 차단해야 함.

### `attendance` (근태 로우데이터)
```
{
  id, employeeId, date,
  checkIn, checkOut, totalHours,
  inputMode: "start-only" | "start-hours" | "start-end",
  type: "정상출근" | "연차" | "반차" | "지각" | "조퇴" | "출장(외근)",
  note,  // 비고 자유입력 (병가/공가/출산휴가/그외)
  editableUntil,  // 당일 자정까지만 매장 수정 가능, 이후 회계팀만
  lastEditedAt, lastEditedBy
}
```

### `naverWorksSync` (대사확인용, 2단계)
```
{ date, employeeId, naverWorksTime, systemTime, matched: bool }
```

## 5. 핵심 워크플로우

### 5-1. 사원등록 — 회계팀 → 인사팀 순차 게이트
```
매장 입력 (이름/주민번호/계좌번호/연락처/입사일 + 주민등록증·통장사본 사진)
  → 계좌번호·주민번호·핸드폰 중복 검증 (중복 시 등록 자체 차단)
  → 회계팀 확인 대기 상태
  → 회계팀이 확인 버튼 클릭 (accountingConfirmed = true)
      ※ 이 시점 전까지는 인사팀 조회 쿼리에서 아예 제외되어야 함
  → 인사팀 확인 대기 상태로 전환 (보건증·근로계약서 사진 첨부 여부와 무관하게, 회계팀 확인 전에는 노출 안 됨)
  → 인사팀이 확인 버튼 클릭 (hrConfirmed = true)
  → 등록 확정, 이때부터 근태입력 대상 명단에 포함
```
- 사원등록 미완료(확정 전) 상태인 사번은 근태입력 화면 드롭다운에서 제외되어야 함
- 필수항목(성명/주민번호/연락처/입사일)만 있으면 등록 가능, 계좌번호/직책/부서/퇴사일은 추후 입력 가능(미입력 시 화면 빨간색 표시)
- 사진 첨부파일(주민등록증/통장사본/보건증/근로계약서)은 **30일 보관 후 자동 삭제** (Cloud Function 배치 또는 Storage lifecycle rule)

### 5-2. 근태입력
- 근무시간 입력 방식 3가지를 모두 지원해야 함
  1. 시작시간만 입력 → 자동 10시간 근무로 종료시간 계산
  2. 시작시간 + 총 근무시간 입력 → 종료시간 자동 계산
  3. 시작시간 + 종료시간 입력 → 총 근무시간 자동 계산
- 휴게시간은 별도 차감하지 않음 — 출근~퇴근 시간을 그대로 급여 계산에 사용
- **수정 권한**: 당일 입력분은 매장이 수정 가능, 당일이 지나면 회계팀만 수정 가능
- 근태구분: 정상출근/연차/반차/지각/조퇴/출장(외근) 드롭다운 + "비고"에 드문 사유(병가/공가/출산휴가 등) 자유입력

### 5-3. 급여 계산 로직 (2단계 이후 본격 구현)
- 자정을 넘는 근무가 없으므로, **일 단위 순차 규칙**으로 계산: 휴일 여부 판정 → 8시간 초과분 연장가산(1.5배, 그 이상 구간은 재확인 필요) → 야간가산(22시~06시, 1.5배)
- 공휴일 근무 시 8시간까지 1.5배, 8시간 초과분은 2배 (정직원 기준. 알바는 공휴일 근무 시 1.5배, 주말 근무는 가산 없음)
- 아르바이트 시급에는 **주휴수당이 이미 포함**되어 있음 — 별도 주휴수당을 추가 지급하는 구조가 아님
- 정직원 연차: 입사일 기준 자동계산 (1년 미만은 개근월수만큼, 1년 이상은 15일+2년마다 1일, 최대 25일) 하되, 본사가 확인 후 예외(육아휴직 등)만 수동 오버라이드

### 5-4. 알바 주 15시간 관리 (실시간 경고)
- 목적: 주휴수당·퇴직금 발생 조건 회피
- 주간(월~일) 누적 실근로시간을 실시간 계산, 13시간 이상부터 경고, 15시간 임박/도달 시 강조 표시
- 반드시 **스케줄이 아닌 실제 출퇴근 기록 기준**으로 계산해야 함

### 5-5. 4대보험 국민연금 대상 자동 표시 (일용직/알바)
- 아래 중 1개 이상 해당 시 국민연금 가입대상으로 표시 (매월 리셋되는 판정)
  - 월 8일 이상 근무
  - 월 60시간 이상 근무
  - 월소득 220만원 이상
- (참고: 근로기준법상 국민연금법 시행령 제2조 기준, 월 단위 판정으로 재확인 완료된 값)

### 5-6. 퇴직금 대상 판정 및 알림
- 발생 요건 (둘 다 충족 시): **계속근로기간 1년 이상 AND 4주 평균 1주 소정근로시간 15시간 이상**
- 알림: 입사 후 만 1년 도래 기준 8개월 차(4개월 전), 10개월 차(2개월 전) 두 번 인사팀에 알림

### 5-7. 일용직 장기 미근무 알림
- 최종근무일로부터 근태 기록이 2주간 없으면 현장+본사에 알림 (퇴직 처리 확인용)

### 5-8. 네이버웍스 출퇴근 대사 (2단계)
- 네이버웍스 출퇴근 기록과 시스템 입력 근태를 비교해 불일치 표시

## 6. 아직 결정되지 않은 사항 (구현 전 실제 담당자와 재확인 필요)

1. 연차 산정 기준: 회계연도 일괄 부여 vs 입사일 기준 개별 산정 (초기 논의와 실제 미팅 내용이 다르게 나왔던 부분 — 최종 확정 필요)
2. 일용직 → 아르바이트 전환 기준 (연속 근무 일수 기준)
3. 일용직의 주휴수당 발생 여부 및 조건
4. 일용직 시급이 상수(고정값)인지 매일 변동되는 값인지
5. 정직원 근태의 매장입력 → 본사승인 워크플로우 적용 여부
6. 알바 주간 경고 임계값의 정확한 숫자, 임계값 도달 시 실제 조치(경고만 vs 스케줄 제한)
7. 아르바이트 하루 근무시간 초과 시 가산(1.5배) 적용 기준 시간 (실제 미팅 녹취에서 수치가 불명확했음)
8. 장기요양보험료 정확한 요율 (이 시스템 스코프 밖일 가능성 있으나 참고용으로 논의됐음)

## 7. 요청사항 (Claude Code에게)

1. 먼저 `payroll_flow_prototype.jsx`를 리뷰하고, 동일한 화면 구조·인터랙션·디자인 톤을 유지하면서 Firebase 프로젝트로 스캐폴딩해주세요.
2. Firestore 데이터 모델은 위 4번 섹션을 기준으로 하되, 더 나은 구조가 있다면 제안해주세요.
3. 1단계 범위(사원등록 게이트 워크플로우, 근태입력, 필수 검증 로직)부터 먼저 구현하고, 급여 계산·대시보드·알림은 2단계로 남겨주세요.
4. Firestore 보안 규칙에서 "매장은 자기 매장 데이터만, 등록 후 수정 불가", "회계팀 확인 전에는 인사팀이 조회 불가" 같은 권한 로직을 정확히 반영해주세요.
5. 6번 섹션의 미결 사항은 코드에 하드코딩하지 말고, 나중에 값만 바꿔 끼울 수 있도록 설정값(config)으로 분리해주세요.
